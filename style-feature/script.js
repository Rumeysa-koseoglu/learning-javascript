//?style feature
//we can give properties to the element we select, using the style object;
const todo = document.querySelector(".todo-list");

todo.style.color = "red"; //*we used the style object and gave the element red color,
//also we can use all css styling properties like backgroundColor, padding, border etc.